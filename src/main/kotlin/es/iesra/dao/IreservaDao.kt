package es.iesra.dao
import java.io.File
import java.io.IOException
import es.iesra.servicio.ReservaService

/**
 * Interfaz genérica para operaciones CRUD.
 * T: Tipo de la entidad (ej. Reserva)
 * K: Tipo del identificador (ej. Int)
 */

/*
interface IDao<T,K> {
    fun crear(entidad: T): Boolean
    fun mostrarTodo(): List<T>
    fun actualizar(entidad: T): Boolean
    fun borrar(id: K): Boolean
}
 */